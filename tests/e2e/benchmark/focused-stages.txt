# Default stages this PR's perf comment will benchmark when BENCH_STAGES is
# not set explicitly. The CI workflow overrides this with separate host PHP
# and PHP.wasm stage lists so each native/userland pair is like-for-like.
db-pull
db-apply
playground-sqlite-db-pull
playground-sqlite-db-apply
